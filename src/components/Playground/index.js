import React from 'react';
import { MarketPerformance } from '../MarketPerformance';

export const Playground = () => {
      return (
            <main>
                  <MarketPerformance />
            </main>
      )
}